# Lsky Pro 项目运行步骤

## 📋 前置检查

### 1. 确认已安装的软件
- ✅ PHP 8.1.25 (XAMPP已安装)
- ✅ Composer 2.8.12
- ⚠️ Node.js (需要检查)
- ⚠️ MySQL (XAMPP已包含，需要启动)

### 2. 检查PHP路径
```bash
# 使用完整路径运行PHP命令
"C:\Program Files\xampp8.1\php\php.exe" -v
```

## 🚀 运行步骤

### 步骤1: 配置环境变量（解决乱码问题）

**方法一：临时设置（推荐）**
```powershell
# 在PowerShell中设置编码
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONIOENCODING = "utf-8"
```

**方法二：使用CMD而不是PowerShell**
```cmd
# 打开CMD（命令提示符）而不是PowerShell
# CMD对中文支持更好
```

**方法三：添加到系统PATH**
1. 右键"此电脑" → 属性 → 高级系统设置
2. 环境变量 → 系统变量 → Path
3. 添加：`C:\Program Files\xampp8.1\php`
4. 重启终端

### 步骤2: 检查并安装依赖

```bash
# 进入项目目录
cd d:\frontend\main\php\lsky-image

# 检查Composer依赖（如果vendor目录已存在，可能不需要重新安装）
composer install

# 检查Node.js依赖
npm install
```

### 步骤3: 配置环境文件

```bash
# 如果.env文件不存在，从示例文件复制
copy .env.example .env

# 生成应用密钥
"C:\Program Files\xampp8.1\php\php.exe" artisan key:generate
```

### 步骤4: 配置数据库

1. **启动MySQL服务**
   - 打开XAMPP控制面板
   - 启动MySQL服务

2. **创建数据库**
   ```sql
   CREATE DATABASE lsky_pro CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. **配置.env文件**
   ```env
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=lsky_pro
   DB_USERNAME=root
   DB_PASSWORD=
   ```

### 步骤5: 运行数据库迁移和安装

```bash
# 运行安装命令（Lsky Pro特有的安装方式）
"C:\Program Files\xampp8.1\php\php.exe" artisan lsky:install --connection=mysql --host=127.0.0.1 --port=3306 --database=lsky_pro --username=root --password=
```

### 步骤6: 编译前端资源

```bash
# 开发模式
npm run dev

# 或生产模式
npm run production
```

### 步骤7: 启动开发服务器

```bash
# 使用Laravel内置服务器
"C:\Program Files\xampp8.1\php\php.exe" artisan serve

# 或指定端口
"C:\Program Files\xampp8.1\php\php.exe" artisan serve --port=8000
```

### 步骤8: 访问项目

打开浏览器访问：
- http://localhost:8000
- 或 http://127.0.0.1:8000

## 🔧 解决乱码问题

如果遇到乱码输出，尝试以下方法：

### 方法1: 使用CMD而不是PowerShell
```cmd
# 打开CMD（命令提示符）
# 在CMD中运行命令
cd d:\frontend\main\php\lsky-image
"C:\Program Files\xampp8.1\php\php.exe" artisan serve
```

### 方法2: 创建批处理文件
创建 `run.bat` 文件：
```batch
@echo off
chcp 65001
cd /d "d:\frontend\main\php\lsky-image"
"C:\Program Files\xampp8.1\php\php.exe" artisan serve
pause
```

### 方法3: 使用Git Bash
```bash
# 在Git Bash中运行
cd /d/frontend/main/php/lsky-image
"/c/Program Files/xampp8.1/php/php.exe" artisan serve
```

## ⚠️ 常见问题

### 1. 乱码问题
- 使用CMD而不是PowerShell
- 或使用Git Bash
- 或创建批处理文件

### 2. PHP命令找不到
- 使用完整路径：`"C:\Program Files\xampp8.1\php\php.exe"`
- 或添加到系统PATH

### 3. 数据库连接失败
- 确保MySQL服务已启动
- 检查数据库用户名和密码
- 确认数据库已创建

### 4. 前端资源未编译
- 运行 `npm run dev` 或 `npm run production`
- 确保Node.js已安装

## 📝 快速启动脚本

创建 `start.bat` 文件：
```batch
@echo off
chcp 65001
echo 正在启动Lsky Pro...
cd /d "d:\frontend\main\php\lsky-image"
echo 启动开发服务器...
"C:\Program Files\xampp8.1\php\php.exe" artisan serve
pause
```

双击运行即可启动项目！






